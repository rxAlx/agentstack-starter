FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn pydantic

COPY anp/marketplace.py registry.py  

EXPOSE 8080

CMD ["uvicorn", "registry:app", "--host", "0.0.0.0", "--port", "8080"]